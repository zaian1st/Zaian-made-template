name: CI Pipeline 🚀

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository 📂
      uses: actions/checkout@v3

    - name: Set up Python 🐍
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies 📦
      run: |
        python -m pip install --upgrade pip
        pip install pytest pandas pdfplumber requests openpyxl

    - name: Run Tests 🧪
      run: |
        cd project  # Navigate to the project directory
        chmod +x test.sh
        ./test.sh

    - name: 🎉 All Done!
      if: always()
      run: echo "🎉 All steps in the CI pipeline have been completed."
